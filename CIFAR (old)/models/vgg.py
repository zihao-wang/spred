'''VGG11/13/16/19 in Pytorch.'''
import torch
from torch import nn as _nn

from .sparse_modules import SpredConv, SpredLinear

_nn.Conv2d = SpredConv
_nn.Linear = SpredLinear

cfg = {
    'VGG11': [64, 'M', 128, 'M', 256, 256, 'M', 512, 512, 'M', 512, 512, 'M'],
    'VGG13': [64, 64, 'M', 128, 128, 'M', 256, 256, 'M', 512, 512, 'M', 512, 512, 'M'],
    'VGG16': [64, 64, 'M', 128, 128, 'M', 256, 256, 256, 'M', 512, 512, 512, 'M', 512, 512, 512, 'M'],
    'VGG19': [64, 64, 'M', 128, 128, 'M', 256, 256, 256, 256, 'M', 512, 512, 512, 512, 'M', 512, 512, 512, 512, 'M'],
}


class VGG(_nn.Module):
    def __init__(self, vgg_name):
        super(VGG, self).__init__()
        self.features = self._make_layers(cfg[vgg_name])
        self.classifier = _nn.Linear(512, 10)

    def forward(self, x):
        out = self.features(x)
        out = out.view(out.size(0), -1)
        out = self.classifier(out)
        return out

    def _make_layers(self, cfg):
        layers = []
        in_channels = 3
        for x in cfg:
            if x == 'M':
                layers += [_nn.MaxPool2d(kernel_size=2, stride=2)]
            else:
                layers += [_nn.Conv2d(in_channels, x, kernel_size=3, padding=1),
                           _nn.BatchNorm2d(x),
                           _nn.ReLU(inplace=True)]
                in_channels = x
        layers += [_nn.AvgPool2d(kernel_size=1, stride=1)]
        return _nn.Sequential(*layers)


def test():
    net = VGG('VGG11')
    x = torch.randn(2,3,32,32)
    y = net(x)
    print(y.size())

# test()
